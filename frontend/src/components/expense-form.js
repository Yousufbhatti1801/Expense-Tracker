export function renderExpenseForm(rootEl) {
  rootEl.innerHTML = `
    <form>
      <h3>Add Expense</h3>
      <p>Form inputs will be added in next iteration.</p>
      <button type="button" disabled>Save Expense</button>
    </form>
  `;
}
